let _history: History
let _sessionStorage: Storage

export const setHistory = (history: History) => _history = history
export const setSessionStorage = (sessionStorage: Storage) => _sessionStorage = sessionStorage
